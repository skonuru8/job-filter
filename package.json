{
  "name": "job-filter",
  "version": "0.1.0",
  "type": "module",
  "description": "Hard-filter stage for the job pipeline. See design-v4.md.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
