{
  "name": "job-filter",
  "version": "0.1.0",
  "type": "module",
  "description": "Hard-filter stage for the job pipeline. See design-v4.md.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "p-limit": "^7.3.0"
  }
}
